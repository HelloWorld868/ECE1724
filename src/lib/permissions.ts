export function isOrganizer(role: string) {
    return role === "organizer";
  }
  
  export function isStaff(role: string) {
    return role === "staff";
  }
  
  export function isAttendee(role: string) {
    return role === "attendee";
  }
  